language: node_js

build:
  ci:
    - echo "Hello world"
    - docker ps 
    - git clone https://github.com/scriptnull/u16.git
    - cd u16 && ls
    - docker build -t="scriptnull/xx:dev" .
    - docker run scriptnull/xx:dev ansible
