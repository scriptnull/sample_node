# Language setting
language: node_js


# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:
  pre_ci:
    - echo "Good morning 2"
    - echo "testing"
    - docker pull scriptnull/sample_node:latest

  # pre_ci_boot:
  #   image_name: scriptnull/sample_node
  #   image_tag: latest
  #   pull: false
  #   options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - echo "post ci stage. Finished ! yo"
    #- docker push scriptnull/sample_node
    
integrations:
    - integrationName: ship-docker
      type: docker
      branches:
        only:
          - master

