# Language setting
language: node_js


# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE
    - M2=TWO

build:
  pre_ci:
    - echo "Good morning 2"
    - echo "testing"

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - echo "post ci stage. Finished ! yo"

integrations:
  deploy:
    - integrationName: "Vishnu's AWS"
      type: aws
      target: ebs_docker
      application_name: amazing-app
      s3_bucket_name: amazing-bucket
      env_name: amazing-app-dev
      region: us-east-1
      branches:
        only:
          - master
        except:
          - test
