# Language setting
language: node_js

env:
  global:
    - FOO=FUBU
    
build:
  ci:
    - ps -eax
    - ls -al

integrations:
  notifications:
    - integrationName: email
      type: email
      recipients:
        - vishnu@shippable.com
      on_start: 'always'
      on_success: 'always '
      on_pull_request: 'always'
      on_failure: 'always'
