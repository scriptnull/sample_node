# Language setting
language: node_js


# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU
    
build:
  pre_ci:
    - echo "Good morning 2"
    - echo "testing"

  ci:
    - ps -eax
    - ls -al

  post_ci:
    - echo "post ci stage. Finished ! yo"
    - pip install awsebcli

integrations:
  deploy:
    - integrationName: "Vishnu's AWS"
      type: aws
      target: ebs_paas
      platform: "Node.js"
      application_name: sample-app-vb
      s3_bucket_name: amazing-bucket
      env_name: sample-app-vb-localhost
      region: us-east-1
