# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - USERNAME=ONE
    - USERNAME=TWO

build:
  pre_ci:
    - echo "this is pre ci"

  ci:
   - echo "this is ci $USERNAME"
  
  post_ci:
    - echo "this is post ci"
