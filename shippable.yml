# Language setting
language: node_js


# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=latest
    - FO1=BUBU
    - BOO=GUBU

#  matrix:
#    - M1=ONE
#    - M2=TWO

build:
  pre_ci:
    - echo "Good morning 2"
    - echo "testing"
    - export HELLO_IMAGE="kanthchandra/sample_node"
    - export HELLO_PULL=true
    - export HELLO_OPTS="--privileged=true --net=bridge -e YO=helloman -e BOO=false"

  pre_ci_boot: "$HELLO_IMAGE:$FOO"

  ci:
    - ps -eax
    - ls -al
    - echo $YO
    - echo $BOO

  post_ci:
    - echo "post ci stage. Finished ! yo"

  smoke_test:
    - sudo ls /

