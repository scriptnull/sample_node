language: node_js

build:
  ci:
    - echo "Hello world"
    - docker ps 
    - git clone https://github.com/dry-dock/u16phpall.git
    - cd u16phpall && ls
    - sed -i 's/{{%TAG%}}/master/g' Dockerfile
    - docker build -t="scriptnull/xx:dev" .
