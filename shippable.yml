language: node_js

build:
  ci:
    - echo "Hello world"
    - MYVERSION=`date +%s`
  
integrations:
  notifications:
    - integrationName: scriptnull-ci
      type: webhook
      payload:
        - versionName=$MYVERSION
      on_success: always
