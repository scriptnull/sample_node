resources:
  - name: sn-b-myapp3
    type: image
    integration: scriptnull-docker
    pointer:
      sourceName: "scriptnull/sample_node"
      isPull: true
    seed:
      versionName: "myapp3"

  - name: sn-b-myapp2
    type: image
    integration: scriptnull-docker
    pointer:
      sourceName: "scriptnull/sample_node"
      isPull: true
    seed:
      versionName: "myapp2"

  - name: sn-b-dk
    type: image
    integration: scriptnull-docker
    pointer:
      sourceName: "scriptnull/debian"
      isPull: true
    seed:
      versionName: "driftking"

  - name: sn-b-ep
    type: image
    integration: scriptnull-docker
    pointer:
      sourceName: "scriptnull/debian"
      isPull: true
    seed:
      versionName: "entrypoint"

  - name: sn-b-docker-opts-1
    type: dockerOptions
    version:
      memory: 100
      user: "driftking"
      workingDir: "/app/makemeworking"
      entryPoint: "/usr/bin/node"
      cmd: "job.js"
      volumes:
        - "/hhjj"
        - "/root/home:/inso_ro:ro"
      volumesFrom:
        - "vol_con:ro"
      network: bridge
      readOnlyRootFilesystem: true
      labels:
        thiscontainer: "is awesome"
      privileged: true
      dnsServers:
        - "127.0.0.1"
      dnsSearch:
        - "127.0.0.1"

  - name: sn-b-ddc-cluster
    type: cluster
    integration: scriptnull-ddc
    pointer:
      sourceName : "some-random-name" #name of the cluster we are deploying

  - name: sn-b-tri-cluster
    type: cluster
    integration: scriptnull-triton
    pointer:
      sourceName : "some-name" #name of the cluster we are deploying
      region : "us-east-2"

  - name: sn-b-gke-cluster
    type: cluster
    integration: scriptnull-gke
    pointer:
      sourceName : "small-cluster" #name of the cluster we are deploying
      region : "us-east1-d"

  - name: sn-b-ecs-lb-cluster
    type: cluster
    integration: scriptnull-aws
    pointer:
      sourceName : "loadBalanceTest" #name of the cluster we are deploying
      region: "us-east-1"

  - name: sn-b-dcl-cluster
    type: cluster
    integration: scriptnull-dcl
    pointer:
      sourceName : "cluster-dcl"
