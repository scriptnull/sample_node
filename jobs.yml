jobs:
  - name: ecs-man
    type: manifest
    steps:
      - GET: ecs-nginx
  
  - name: ecs-deploy
    type: ecsDeploy
    options:
      deployMethod: bluegreen
    steps:
      - GET: ecs-man
      - GET: ecs-cluster
