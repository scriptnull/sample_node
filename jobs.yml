jobs:
  - name: tri-manifest
    type: manifest
    steps:
      - GET: tri-sn
      # - DO: tri-nginx

  - name: tri-deploy
    type: tripubDeploy
    steps:
      - GET: tri-manifest
      - GET: tri-cluster
      - GET: tri-ecs-deploy
    
  - name: tri-ecs-manifest
    type: manifest
    steps:
      - GET: tri-nginx

  - name: tri-ecs-deploy
    type:  ecsDeploy
    steps:
      - GET: tri-ecs-manifest
      - GET: tri-ecs-cluster

  - name: ecs-chain-from-tri
    type: ecsDeploy
    steps:
      - GET: tri-deploy
      - GET: tri-ecs-cluster
