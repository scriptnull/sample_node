jobs:
  - name: ecs-man
    type: manifest
    steps:
      - GET: ecs-sn
  
  - name: ecs-deploy
    type: ecsDeploy
    options:
      deployMethod: green
    steps:
      - GET: ecs-man
      - GET: ecs-cluster
      
  - name: ecs-man-1
    type: manifest
    steps:
      - GET: ecs-nginx
  
  - name: ecs-deploy-1
    type: ecsDeploy
    options:
      deployMethod: green
    steps:
      - GET: ecs-man-1
      - GET: ecs-cluster
