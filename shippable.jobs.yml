jobs:
  - name: sn-man
    type: manifest
    steps:
      - GET: sn-nginx
      - GET: sn-trigger
      # - GET: sn-opts
      # - GET: sn-replicas

  - name: loc-gke
    type: gkeDeploy
    steps:
      - GET: sn-man
      - GET: sn-gke

  - name: ecs-man-1
    type: manifest
    steps:
      - GET: sn-nginx
      - GET: ecs-trigger
      # - GET: sn-opts
      # - GET: sn-replicas

  - name: loc-ecs-1
    type: gkeDeploy
    steps:
      - GET: ecs-man
      - GET: sn-ecs
