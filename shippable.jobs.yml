jobs:
  - name: tri-manifest-A
    type: manifest
    steps:
      - GET: tri-sn
      - GET: tri-trigger

  - name: tri-manifest-B
    type: manifest
    steps:
      - GET: tri-nginx

  - name: tri-deploy-bg
    type: tripubDeploy
    options:
      deployMethod: blueGreen
    steps:
      - GET: tri-manifest-A
      - GET: tri-cluster
    
