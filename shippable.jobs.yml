jobs:
  - name: sn-man
    type: manifest
    steps:
      - GET: sn-sn
      - GET: sn-trigger
      # - GET: sn-opts
      # - GET: sn-replicas

  - name: x1-gke
    type: gkeDeploy
    steps:
      - GET: sn-man
      - GET: sn-gke

  - name: b-ecs-man-1
    type: manifest
    steps:
      - GET: sn-sn
      - GET: ecs-trigger

  - name: b-ecs-man-2
    type: manifest
    steps:
      - GET: sn-nginx
      - GET: ecs-trigger

  - name: ecs-deploy
    type: ecsDeploy
    steps:
      - GET: b-ecs-man-1
      - GET: b-ecs-man-2
      - GET: sn-ecs

  - name: b-tri-1-man
    type: manifest
    steps:
      - GET: sn-sn
      - GET: tri-trigger

  - name: b-tri-deploy
    type: tripubDeploy
    steps:
      - GET: b-tri-1-man
      - GET: sn-tri

###################### GKE Testing starts here
  - name: fb-man
    type: manifest
    steps:
      - GET: sn-sn
      - GET: fb-trigger
      - GET: gke-opts
      - GET: good-params

  - name: fb-gke
    type: gkeDeploy
    options:
      deployMethod: upgrade
    steps:
      - GET: fb-man
      - GET: sn-gke
